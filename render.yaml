services:
  - type: web
    name: docmind-api
    runtime: python
    pythonVersion: "3.12.0"
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn api.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: APP_SECRET_KEY
        generateValue: true
      - key: WEB_ORIGINS
        value: https://docmind.vercel.app   # update after Vercel assigns your URL
      - key: GROQ_API_KEY
        sync: false                          # set manually in Render dashboard
      - key: DATA_DIR
        value: .docmind_data
      - key: PYTHON_VERSION
        value: "3.12.0"
